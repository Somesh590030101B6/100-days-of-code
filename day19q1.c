#include <stdio.h>
int main()
{
    int a,b,lcm,i;
    printf("enter numbers:");
    scanf("%d %d",&a,&b);
    if (a>b)
    {
        for (i=1;i<=b;i++)
        {
            if (a%i==0 &&)
            {
                lcm
            }
        }
    }

}